# k6-resource.yml

apiVersion: k6.io/v1alpha1
kind: K6
metadata:
  name: k6-sample-basic
spec:
  parallelism: 4
  script:
    configMap:
      name: k6-demo-configmap-basic
      file: single_request_complete_script.js

      
